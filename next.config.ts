import { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "source.unsplash.com",
      "static.vecteezy.com",
      "chatgpt.com",
      "images.unsplash.com",
      "unsplash.com",
      "ui-avatars.com",
      "plus.unsplash.com",
    ],
  },
};

export default nextConfig;
